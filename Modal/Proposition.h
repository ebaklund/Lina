#pragma once

#include <cstdint>
#include <array>
#include <vector>

namespace Modal
{
    template<typename T, uint32_t N>
    class Proposition
    {
    private:
        std::array<T, N> _vec;

    public:
    };
}
